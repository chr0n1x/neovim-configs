require("zen-mode").setup(
  {
    window = {
      backdrop = 0.80,
      width = 120,
      height = 0.80
    },

    options = {
      ruler = true,
      showcmd = true,
    },

    gitsigns = { enabled = true }
  }
)
